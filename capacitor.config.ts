import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'net.spx.app',
  appName: 'kanji-mem',
  webDir: 'dist/kanji-mem/browser'
};

export default config;
